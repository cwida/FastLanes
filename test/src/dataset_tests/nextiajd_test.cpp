#include "data/NextiaJD.hpp"
#include "fastlanes.hpp"
#include "fls_tester.hpp"

namespace fastlanes {

TEST_F(FastLanesReaderTester, NextiaJD_Chicago_Crimes_2001_to_2004) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::Chicago_Crimes_2001_to_2004, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_Chicago_Crimes_2005_to_2007) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::Chicago_Crimes_2005_to_2007, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_Chicago_Crimes_2012_to_2017) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::Chicago_Crimes_2012_to_2017, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_Consumer_Complaints) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::Consumer_Complaints, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_Homo_sapiens_GRCh38_92) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::Homo_sapiens_GRCh38_92, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_all_anonymized_2015_11_2017_03) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::all_anonymized_2015_11_2017_03, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_artists) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::artists, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_az) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::az, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_business_licences) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::business_licences, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_comments) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::comments, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_econbiz) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::econbiz, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_eo2) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::eo2, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_eo3) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::eo3, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_examiner_date_text) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::examiner_date_text, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_examiner_date_tokens) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::examiner_date_tokens, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_glassdoor_photos) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::glassdoor_photos, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_news_week_17aug24) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::news_week_17aug24, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_otp) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::otp, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_plays) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::plays, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_previous_application) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::previous_application, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_property_tax_report) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::property_tax_report, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_reviews_detailed) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::reviews_detailed, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_reviews_summary) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::reviews_summary, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_saudi_hourly_weather_data) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::saudi_hourly_weather_data, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_train_balanced_sarcasm) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::train_balanced_sarcasm, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_trainView) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::trainView, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_us_perm_visas) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::us_perm_visas, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_wowah_data) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::wowah_data, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

TEST_F(FastLanesReaderTester, NextiaJD_yellow_tripdata_2019_01) {
	const vector<n_t> constant_cols                 = {/* ... */};
	const vector<n_t> equal_cols                    = {/* ... */};
	const vector<n_t> one_to_one_mapped_col_indexes = {/* ... */};
	AllTest(NextiaJD::yellow_tripdata_2019_01, constant_cols, equal_cols, one_to_one_mapped_col_indexes);
}

} // namespace fastlanes
